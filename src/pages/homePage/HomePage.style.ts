import { css } from '@emotion/react';

import { Theme } from '@/styles/theme';

export const myInfoContainerStyle = css({
  width: '100%',

  padding: '12px',
  paddingTop: '24px',
});

export const boxStyle = {
  width: '100%',

  padding: '24px 12px',
  marginTop: '12px',

  backgroundColor: Theme.color.white,
};
